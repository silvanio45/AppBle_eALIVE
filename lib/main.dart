import 'package:appble/App.dart'; // Ensure this file contains the AppWidget class
import 'package:flutter/material.dart';
// import 'package:appble/App_controller.dart'; // Comment this if not needed
// import 'app.dart'; // Remove this if AppWidget is already imported above

void main() {
  runApp(AppWidget());
}
